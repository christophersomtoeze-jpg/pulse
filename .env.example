# Supabase public client configuration (safe for browser use)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Do NOT put an OpenAI secret key in VITE_* variables or the browser.
# AI requests should go through a secure server/edge function.
